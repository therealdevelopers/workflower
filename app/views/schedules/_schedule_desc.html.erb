<%= link_to schedule.title, user_schedule_path(user_id: current_user.id, id: schedule.id), class: "button"%>
<%= link_to 'rename', edit_user_schedule_path(user_id: current_user.id, id: schedule.id), class: "button" %>
<%= link_to 'delete', user_schedule_path(user_id: current_user.id, id: schedule.id), method: :delete, data: {confirm: 'Are you sure?'}, class: "button"%>
